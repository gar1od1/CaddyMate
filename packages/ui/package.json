{
  "name": "@caddymate/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens (colours, spacing, type scale) for mobile and web.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'tokens only'",
    "test": "echo 'tokens only'"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
