# NNN — Title in sentence case

Status: Proposed | Accepted | Superseded by NNN
Date: YYYY-MM-DD
Scope: SPEC §x.y. Code: `path/to/module`, `path/to/other`. Versions: `SOME_MODEL_VERSION = n` (if
a model changed).

## Context

What the SPEC says (or doesn't), what forced a choice, and any constraint that matters (runtime,
data we have, performance). Two or three sentences.

## Decision

What we do, stated plainly enough to check the code against it. Tables are good for chosen values:

| Item | Value | Rationale |
| ---- | ----- | --------- |
|      |       |           |

## Alternatives considered

- **Option** — why not.

## Consequences

- What this makes easier or harder; what now has to stay in sync.
- Migrations, version bumps or data back-fills it implies.
- How it is verified (tests, property tests, parity tests) and what is not yet verified.
